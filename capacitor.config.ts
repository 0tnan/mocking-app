import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.mocking.app",
  appName: "mocking-app",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
